[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
# Force npm install instead of npm ci by removing lock file first
cmds = ["rm -f package-lock.json; npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
