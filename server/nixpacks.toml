[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
# Force npm install instead of npm ci
cmds = [
  "rm -f package-lock.json || true",
  "npm install --package-lock=false || npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
