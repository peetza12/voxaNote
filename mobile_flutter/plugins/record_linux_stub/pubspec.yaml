name: record_linux
description: Local stub implementation of record_linux for non-Linux targets.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  record_platform_interface: ^1.4.0


